/*
 * Questão 3:
 * Crie uma classe para representar uma data que inclui três informações como variáveis de instância: mês (int), dia (int) e ano (int). 
 * A classe deve ter métodos get e set para cada variável e um construtor que inicializa as variáveis e assume que os valores fornecidos são corretos. 
 * Forneça um método displayData que exibe o dia, o mês e o ano separados por barras normais (/).
 * Escreva uma classe principal de teste chamada DataTeste que demonstra as capacidades da classe Data.
 */

public class App {
    public static void main(String[] args) {
        // Chama o método main da classe DataTeste para executar o teste
        DataTeste.main(args);
    }
}
